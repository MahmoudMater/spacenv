version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: envspace_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: envspace
      POSTGRES_PASSWORD: envspace_secret
      POSTGRES_DB: envspace
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U envspace']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: envspace_redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redisdata:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
